# dotfiles
My config/dotfiles

### Usage
requirements: `stow, kitty, zsh, fzf`

1. clone this repo to your home folder:
```sh
cd ~ && git clone --recurse-submodules https://github.com/beshralghalil/dotfiles
```
2. install the configs you want
```sh
# e.g
stow zsh
```
